[package]
name = "entity-remote"
description = "An entity store over a wire: the protocol, transport-agnostic, with the network at the edge."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
entity-core = { path = "../entity-core", version = "0.7.0" }
entity-store = { path = "../entity-store", version = "0.7.0" }
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true
