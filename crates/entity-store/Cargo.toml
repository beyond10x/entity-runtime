[package]
name = "entity-store"
description = "Provider interfaces for keeping entity instances and their events, outside the kernel."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
entity-core = { path = "../entity-core", version = "0.17.7" }
serde.workspace = true
serde_json.workspace = true
# Cross-process advisory locks release automatically on process exit; supports the MSRV and Windows.
fs2 = "0.4.3"

[lints]
workspace = true
