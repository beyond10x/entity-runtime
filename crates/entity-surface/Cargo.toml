[package]
name = "entity-surface"
description = "Deterministic public schemas, contracts and documentation projected from entity definitions"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
entity-core = { path = "../entity-core", version = "0.17.7" }
entity-graph = { path = "../entity-graph", version = "0.17.7" }
serde_json.workspace = true
serde_yaml_ng.workspace = true

[lints]
workspace = true

[dev-dependencies]
# Exercise generated event payloads against JSON Schema without HTTP or file resolution.
jsonschema = { version = "=0.49.9", default-features = false }
